import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.evangeliodiario.app',
  appName: 'evangeliodeldia-app',
  webDir: 'dist'
};

export default config;
